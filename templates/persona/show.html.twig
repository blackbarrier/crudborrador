{% extends 'base.html.twig' %}

{% block title %}Consulta{% endblock %}

{% block encabezado %}
    Consulta
{% endblock %}

{% block main %}


<div style="max-width: 60%; margin:auto">
    <table class="table table-sm table-bordered">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ persona.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ persona.Nombre }}</td>
            </tr>
            <tr>
                <th>Dni</th>
                <td>{{ persona.dni }}</td>
            </tr>
            <tr>
                <th>Sexo</th>
                <td>{{ persona.sexo ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>
</div>
   
    

    <a class="btn btn-secondary" href="{{ path('app_persona_edit', {'id': persona.id}) }}">Editar</a>
    
    {{ include('persona/_delete_form.html.twig') }}

    
{% endblock %}
